spring:
  application:
    name: partner-service
  data:
    mongodb:
      uri: ${MONGODB_URI}

server:
  port: 8084
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: true