spring:
  application:
    name: partner-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/TrackMate

server:
  port: 8084
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: true